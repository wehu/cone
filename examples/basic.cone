module xxx

type A<a> {
   B(a)
   C
}

effect e<a> {
   fun test<a>(a) : a
}

effect ee {
   fun test2(i32) : i32
}

fun main<a>(b: a) : io unit {
   var a = 0
   a = a + 1 * 5
   a = a * 5
   handle e<a> {
      test(b)
   } with {
      fun test(a: a) : a {
         print(a)
         resume(a)
         a
      }
   }
   unit
   print(a)
}

