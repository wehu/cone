module xxx

type A<a> {
   B(a)
   C
}

effect e {
   fun test<a>(a) : a
}

effect ee {
   fun test2(i32) : i32
}

fun main<a>() : io unit {
   var a = 0
   a = a + 1 * 5
   a = a * 5
   handle e {
      test(0.1)
      test(C<a>)
   } with {
      fun test<a>(a: a) : e a {
         print(a)
         a
      }
   }
   unit
   print(a)
}

