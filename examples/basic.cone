module xxx

type A<a> {
   B(a)
   C
}

effect e<a> {
   fun test(a) : a
}

effect ee {
   fun test2(i32) : i32
}

fun main<a>() : io<a> unit {
   var a = 0
   a = a + 1 * 5
   a = a *5
   handle e<a> {
     test(0.1)
     test(1)
   } with {
      fun test<a>(a11: a) : e<a> a {
         print(a11)
         a11
      }
   }
   unit
   print(a)
}

